# Operation Control System Configuration

# ProxyCache connection
proxy.url=http://localhost:8080
ws.url=ws://localhost:8080/stream/operator

# Logging
logging.level.root=INFO
logging.level.com.sitm.mio.operationcontrol=DEBUG

# Application
app.name=SITM-MIO Operation Control
app.version=1.0.0

# Observer System Configuration

# ProxyCache connection
proxy.url=http://localhost:8080
ws.url=ws://localhost:8080/stream/observer

# Logging
logging.level.root=INFO
logging.level.com.sitm.mio.observer=DEBUG

# Application
app.name=SITM-MIO Observer
app.version=1.0.0

# Analytics
analytics.updateInterval=5000
analytics.historyRetention=7days

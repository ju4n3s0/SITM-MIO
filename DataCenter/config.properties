# ============================================
# DataCenter Configuration
# ============================================

# Database Settings
# Change these to match your PostgreSQL setup
database.url=jdbc:postgresql://10.147.20.62:5432/SITM-MIOJM
database.user=postgres
database.password=postgres

# Server Settings
server.host=0.0.0.0
server.port=9090

# Datagram Source Strategy
# Options: DATABASE (historic data) or UDP (real-time simulation)
datagram.source=UDP

# UDP Receiver Settings (for real-time mode)
# Port for receiving bus telemetry datagrams
udp.receiver.port=1000

# Database Polling Settings (for historic mode)
# Interval in milliseconds between database polls
database.poll.interval=10000

# Logging
log.level=INFO
